# csv_summary_generator
CSV Reader Summary Generator is a Python project that reads data from a CSV file and generates a summary report. It calculates total records, sum and average of numeric fields, and group-wise summaries. The project demonstrates file handling, data processing, and basic statistical analysis using Python’s built-in csv module.
